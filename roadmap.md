# lxa Roadmap

`lxa` keeps the roadmap short, future-focused, and tied to release milestones. All implementation work must follow `AGENTS.md`: no crashes, 100% coverage, RKRM/NDK validation, third-party libraries kept off ROM, and roadmap updates on every completed phase.

---

## Next Release Focus

### Phase 79 complete (`0.8.1`)

Phase 79 closes the remaining `keyboard.device` surface and keeps it aligned with the hosted input stack.

- [x] Implemented the documented public command set: `CMD_CLEAR`, `KBD_READEVENT`, `KBD_READMATRIX`, `KBD_ADDRESETHANDLER`, `KBD_REMRESETHANDLER`, `KBD_RESETHANDLERDONE`
- [x] Matched `KBD_READEVENT` behavior for raw-key event chaining, held/numeric-pad qualifier reporting, pending-read completion, and abort handling
- [x] Matched `KBD_READMATRIX` matrix sizing/layout semantics with direct 32-byte snapshot coverage against the hosted raw-key stream
- [x] Shared hosted keyboard events from Intuition into `keyboard.device`, keeping the stack aligned with `input.device`, console raw-key consumers, and `keymap.library`
- [x] Added direct regression coverage under `Tests/Devices/Keyboard` and `devices_gtest` for open/read/abort/reset-handler flows
- [x] Factored queue, matrix, and reset-handler bookkeeping into compact private helpers and reused buffered snapshots during bursty reads

---

### Phase 80: Post-0.8.0 cleanup and acceleration

Goal: pay down the architecture and performance follow-up identified during the Phase 78 compatibility sweep without reopening already-closed API coverage.

- [x] Exec/DOS core: unify resident registration, interrupt-vector management, packet transport, and current-process access so startup and messaging paths cannot drift; current-task/current-process helpers, shared resident/device registration, centralized interrupt-vector/server-chain bookkeeping, and DOS packet send/wait/reply/result propagation now share the common path
- [x] Graphics/Layers: centralize viewport, copper, double-buffer, layer ordering, and clip/damage bookkeeping; viewport/color propagation, copper placeholder ownership, bitmap/origin updates, layer z-order rebuilds, clip-region application, and disjoint damage clipping now share common helpers while broader refresh/palette follow-up remains
- [x] Intuition/Workbench/GadTools: separate private UI bookkeeping from public structs, share screen/workbench/app-object helpers, and avoid repeated full-list scans or full-window redraws; GadTools context heads now stay private when windows/refresh paths consume gadget lists, and Workbench app-object handles now reuse a tracked private list while broader redraw/app-helper follow-up remains
- [x] Utility/Locale/IFFParse/Diskfont: split monolithic private state into companions/helpers and add caching for namespace lookup, catalog lookup, parser context matching, and color-font rendering; Utility namespaces now cache recent lookups, locale caches default-locale/catalog hits, iffparse reuses property/local-item context matches, and diskfont caches repeated font opens/scaled lookups while broader helper extraction remains
- [x] Keymap/Timer/Console/Input: share translation and timing helpers across the input stack, then remove repeated scans, conversions, and queue walks in hot paths; rawkey-to-vanilla conversion and system-time normalization now live in shared ROM helpers, while console reads/input buffers reuse common readiness/consume paths with added regression coverage for normalized repeat timings and console driver flows
- [x] Audio/Trackdisk/Expansion: device-private state now stays behind dedicated helpers/private companions, removing global unit tables and keeping allocation/mount bookkeeping out of ROM entry points while preserving ordered wake-up/reply paths
- [x] Math libraries: hosted FFP/IEEE bridge helpers now share centralized host conversion/clamp/compare/register plumbing so the math emucall surface uses one compatibility model with less repeated setup overhead

---

### Phase 80-2: Fix UI Glitches

- [x] GadToolsGadgets Sample: fix rootless mode: a separate rootless windows gets opened, but the actual UI is still drawn on the "Workbench Screen" window
- [x] GadToolsGadgets Sample: Volume setting is not displayed (empty space right of label)
- [x] GadToolsGadgets Sample: UI is incredibly sluggish - for example, there is a noticable lag when clicking the "Click Here" Button that is not present on a real Amiga
- [x] GadToolsGadgets Sample: trying to resize the window is very laggy and leaves window border garbage on screen while resizing
- [x] GadToolsMenu Sample: separators are too wide, parts are drawn outside the menu and leave garbage on screen
- [x] GadToolsMenu Sample: Print submenu indicator is missing
- [x] GadToolsMenu Sample: Print submenu is missing
- [x] GadToolsMenu Sample: Redraw issues: while moving the mouse over opened menus, flickering occurs
- [x] GadToolsMenu Sample: Redraw issues: while moving the mouse over opened menus, lower menu items keep appearing and disappearing
- [x] SimpleGTGadget Sample: Checkbox doesn't react to mouse clicks, no check mark
- [x] SimpleGTGadget Sample: Number gadget is not responsive to keyboard input, cursor does not show when clicked (but entered characters do appear once focus leaves gadget / clicked elsewhere)
- [x] SimpleGTGadget Sample: Cycle gadget is just an empty box, does not respond to any input

---

### Phase 81 complete (`0.8.10`)

Goal: Fully working Directory Opus app. DOpus 4 Manual: /home/guenter/projects/amiga/lxa/src/lxa-apps/DirectoryOpus/DirOpus4Manual.md

- [x] Audit and document the exact DOpus runtime surface (`assigns`, `LIBS:`, `C:`, `L:`, `S:`, and disk-provided dependencies); `doc/app_db.md` now records the `DOpus:`/`LIBS:`/`C:`/`L:`/`S:` layout, bundled third-party libraries and helpers, startup-sequence hand-off, and the currently observed runtime library/device surface from the App-DB copy
- [x] Keep disk-provided dependencies such as `dopus.library`, `powerpacker.library`, and other optional libraries outside ROM scope; `Tests/Exec/Library` now asserts the DOpus bundle libraries remain absent from the ROM resident list and Exec `LibList`, while `README.md` and `doc/app_db.md` document that they must continue to load from disk via `LIBS:`
- [x] Close the remaining launch blockers, including `keyboard.device` and any other startup-time open failures needed to reach the main UI; direct `DirectoryOpus` launch now reaches the first real `DOPUS.1` window, confirming `keyboard.device` is no longer gating startup while currently missing `commodities.library`, `rexxsyslib.library`, and `inovamusic.library` remain non-fatal optional follow-up items
- [x] Replace the old wrapper smoke check with a host-side driver that verifies real launch success criteria; `tests/drivers/apps_misc_gtest.cpp` now launches the real `APPS:DirectoryOpus/bin/DOPUS/DirectoryOpus` binary under `liblxa`, wires the required `DOpus:`/`LIBS:`/`C:`/`L:`/`S:` assigns, and asserts that startup reaches a live DOpus rootless window with the expected 640px-wide main UI while the process remains running
- [x] Re-enabled the live `DirectoryOpus` driver in `tests/drivers/apps_misc_gtest.cpp` and reran the broader app-regression sweep (`apps_misc_gtest`, `asm_one_gtest`, `devpac_gtest`, `kickpascal_gtest`, `maxonbasic_gtest`, `cluster2_*_gtest`, `dpaint_gtest`), keeping the application-driver set green with DOpus included again
- [x] Extended the host-side integration driver infrastructure with explicit `lxa_wait_window_drawn()` readiness checks plus window/gadget introspection (`lxa_get_gadget_count()`, `lxa_get_gadget_info()`, `LxaUITest::GetGadgets()`, `ClickGadget()`), and covered it in `api_gtest` while confirming the DOpus launcher reaches a drawable interactive window under `apps_misc_gtest`
- [x] Parked the still-flaky DOpus file-copy workflow for a later phase; `tests/drivers/apps_misc_gtest.cpp` now keeps the experimental `DirectoryOpusCopiesFile` scenario disabled so the proven launch/runtime coverage and the new UI-driver tooling can ship green and be revisited later without blocking Phase 81

---

### Phase 82: Address manual test results

- [x] Fix headless mode: headless/app-launch configs now default to `rootless_mode = true`, so the Workbench screen stays hidden and only application windows appear on the host side unless a config explicitly opts back into screen-mode rendering.
- [x] In headless mode make sure application windows are wide enough on the X11 side so they can display the whole menu; rootless host windows now widen to the remaining screen width while keeping their logical Amiga dimensions intact, with regression coverage for both the sizing helper and a captured MenuLayout rootless window
- [x] ASMOne no longer crashes on keyboard input; IDCMP RAWKEY messages now provide the documented previous-key snapshot through `IntuiMessage.IAddress`, with regression coverage in `Tests/Console/input_inject` and the live `asm_one_gtest` keyboard scenarios
- [x] KickPascal rootless launches now keep the logical Amiga window geometry while widening the host-side window, so the IDE no longer appears in a portrait-shaped host frame; regression coverage now captures the live rootless KP2 window and asserts the widened host extent stays landscape.
- [x] DPaint V now tolerates the disk-provided `rexxsyslib.library`; the build installs a shared `SYS:Libs/rexxsyslib.library` from the existing stub source, `dpaint_gtest` appends the DPaint `Libs/` drawer to `LIBS:`, and the live launch regression now asserts startup no longer stalls on the missing-library system requester.
- [x] SysInfo no longer opens to a black rootless window; viewport bitmap swaps now keep the embedded `Screen` bitmap plus host refresh metadata aligned, and `apps_misc_gtest` asserts the live SysInfo window draws visible content under headless rootless mode.
- [x] Sample Talk2Boopsi now follows the RKRM flow again: the sample keeps its linked prop/string gadgets visible until the user closes the window, headless interaction coverage verifies it no longer quits early, and a non-rootless pixel regression confirms the prop gadget renders visible content.
- [x] Sample SimpleGadget now renders its boolean gadget border in rootless mode too; Intuition draws user gadgets into the rootless backing bitmap at open time, and `simplegad_gtest` now captures the live headless window to verify the border stays visible there alongside the existing non-rootless pixel regression.
- [x] Sample UpdateStrGad now follows the RKRM activation flow again: the sample explicitly opens as an active window so `IDCMP_ACTIVEWINDOW` updates the string gadget to `Activated`, rootless host-window input is translated back into screen coordinates so key focus/clicks no longer lag behind in SDL rootless mode, and `updatestrgad_gtest` plus rootless-layout unit coverage keep the activation regression covered.


---

### Phase 83: Close `dos.library` stub surface

Goal: eliminate the remaining non-private `dos.library` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [x] Finish `__g_lxa_dos_InitLib()` so DOS library startup no longer logs an unimplemented stub and the resident init path follows Amiga library-init expectations; startup now installs `DOSBase`, clears the private compatibility pointers, allocates `RootNode`/`DosInfo`/the initial task array with failure cleanup, and `Tests/Exec/Library` asserts the initialized DOS base state directly
- [x] Compare and verify the finished `__g_lxa_dos_InitLib()` path against the AROS `dos.library` initialization flow; the startup path now matches the shared public setup AROS performs for `dl_Root`, `rn_TaskArray`, `rn_Info`, the empty CLI list, and `DosInfo` semaphore initialization, while intentionally keeping the lxa-specific private-pointer clearing and separately allocated `RootNode` model that fits the NDK `RootNode` layout
- [x] Implement `LockRecord()` per RKRM/NDK semantics with direct regression coverage; `LockRecord()` now routes through a hosted cooperative record-lock table keyed by file identity plus the originating filehandle, returns documented collision/timeout errors for immediate vs waiting modes, releases locks when the owning handle closes, and `Tests/Dos/LockRecord` plus `Tests/Exec/Library` cover the active entry point directly
- [x] Compare and verify `LockRecord()` behavior against the AROS implementation; AROS forwards `LockRecord()` by rejecting `BNULL` handles and then issuing `ACTION_LOCK_RECORD` with the filehandle's `fh_Type`/`fh_Arg1`, and the lxa path now preserves that public contract with the hosted record-lock table: locks stay cooperative and filehandle-scoped, same-handle relocks succeed, shared-vs-exclusive collisions/timeouts map to the documented `IoErr()` values, and close-driven release remains covered by `Tests/Dos/LockRecord` plus the direct `Tests/Exec/Library` entry-point check
- [x] Implement `LockRecords()` per RKRM/NDK semantics with direct regression coverage; `LockRecords()` now follows the documented all-or-nothing array contract by locking each `RecordLock` entry in order, rolling back any already-acquired entries on the first failure, rejecting a null array with `ERROR_INVALID_LOCK`, and adding direct coverage in `Tests/Dos/LockRecords`, `dos_gtest`, and `Tests/Exec/Library`
- [x] Compare and verify `LockRecords()` behavior against the AROS implementation; AROS implements `LockRecords()` as an ordered `LockRecord()` loop terminated by `rec_FH == BNULL`, applying the same timeout to each attempt and rolling the already-acquired prefix back through `UnLockRecords()` on the first failure, and the lxa path now preserves that public contract with the hosted record-lock table: it treats the null-`rec_FH` terminator the same way, forwards each entry through the same `LockRecord()` semantics, keeps the documented all-or-nothing rollback for earlier entries only, and retains direct coverage in `Tests/Dos/LockRecords`, `dos_gtest`, and `Tests/Exec/Library`
- [x] Implement `UnLockRecord()` per RKRM/NDK semantics with direct regression coverage; `UnLockRecord()` now routes through the hosted record-lock table using the original filehandle plus exact offset/length matching, returns `ERROR_RECORD_NOT_LOCKED` for wrong-handle or mismatched-range unlock attempts, rejects `BNULL` handles with `ERROR_INVALID_LOCK`, and adds direct regression coverage in `Tests/Dos/UnLockRecord`, `dos_gtest`, and `Tests/Exec/Library`
- [x] Compare and verify `UnLockRecord()` behavior against the AROS implementation; AROS implements `UnLockRecord()` by rejecting `BNULL` handles and then forwarding the original filehandle's `fh_Type`/`fh_Arg1` through `ACTION_FREE_RECORD`, and the lxa path now preserves that public contract with the hosted cooperative record-lock table: null handles still fail with `ERROR_INVALID_LOCK`, exact filehandle-plus-range matches release the lock, wrong-handle or mismatched-range unlock attempts report `ERROR_RECORD_NOT_LOCKED`, and the direct coverage in `Tests/Dos/UnLockRecord`, `dos_gtest`, and `Tests/Exec/Library` keeps the aligned semantics pinned down
- [x] Implement `UnLockRecords()` per RKRM/NDK semantics with direct regression coverage; `UnLockRecords()` now walks the `RecordLock` array until the documented null-`rec_FH` terminator, forwards each entry through `UnLockRecord()`, rejects a null array with `ERROR_INVALID_LOCK`, preserves the first failing `IoErr()` from the underlying unlock attempt, and adds direct regression coverage in `Tests/Dos/UnLockRecords`, `dos_gtest`, and `Tests/Exec/Library`
- [x] Compare and verify `UnLockRecords()` behavior against the AROS implementation; AROS implements `UnLockRecords()` as a straight ordered `UnLockRecord()` walk terminated by `rec_FH == BNULL`, returning immediately on the first failing entry and otherwise succeeding without extra array mutation, and the lxa path now preserves that public contract with the hosted cooperative record-lock table: it rejects a null array with `ERROR_INVALID_LOCK`, stops at the documented null-handle terminator, forwards each entry through the same `UnLockRecord()` semantics, and keeps the aligned direct coverage in `Tests/Dos/UnLockRecords`, `dos_gtest`, and `Tests/Exec/Library`
- [x] Implement `SplitName()` per RKRM/NDK semantics with direct regression coverage; `SplitName()` now follows the documented component-splitting contract by copying from `oldpos` until the separator or end-of-string, truncating to `size-1` while still returning the next separator position, rejecting null pointers and invalid size/offset values with the expected `IoErr()` results, and adding direct regression coverage in `Tests/Dos/SplitName`, `dos_gtest`, and `Tests/Exec/Library`
- [x] Compare and verify `SplitName()` behavior against the AROS implementation; AROS `rom/dos/splitname.c` still uses the legacy loop that stops once the caller buffer is full, reproducing the documented V36/V37 truncation bug for overlong components followed by a later separator, while the lxa path intentionally keeps scanning to preserve the NDK-fixed V39 contract and the existing `Tests/Dos/SplitName`, `dos_gtest`, and `Tests/Exec/Library` coverage keeps that divergence explicit
- [x] Implement `SetMode()` per RKRM/NDK semantics with direct regression coverage; `SetMode()` now validates the filehandle, routes interactive `CON:` handles through the hosted console unit to switch between raw and line-buffered reads for modes `1` vs `0`/`2`, rejects unsupported handles or modes with the documented `IoErr()` values, and adds direct regression coverage in `Tests/Console/setmode` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SetMode()` behavior against the AROS implementation; AROS `rom/dos/setmode.c` forwards `SetMode()` directly to the filehandle's handler via `ACTION_SCREEN_MODE`, and its console handler treats non-zero requests as RAW mode while zero returns to buffered console mode, flushing pending buffered input on the CON->RAW transition; the lxa path preserves that public contract for interactive `CON:` handles, intentionally keeps the documented NDK/V47 extension that accepts mode `2` as buffered medium mode, and retains the direct regression coverage in `Tests/Console/setmode` plus the `Tests/Exec/Library` entry-point probe to keep the aligned semantics explicit
- [x] Implement `ChangeMode()` per RKRM/NDK semantics with direct regression coverage; `ChangeMode()` now closes the public stub by routing the DOS entry point through a hosted access-mode bridge, validating `CHANGE_LOCK` vs `CHANGE_FH`, preserving shared-to-exclusive conflict rejection with `ERROR_OBJECT_IN_USE`, and adding direct regression coverage in `Tests/Dos/ChangeMode`, `dos_gtest`, and `Tests/Exec/Library`
- [x] Compare and verify `ChangeMode()` behavior against the AROS implementation; AROS `rom/dos/changemode.c` validates only the public `CHANGE_LOCK`/`CHANGE_FH` selector, then forwards `ACTION_CHANGE_MODE` to the object's handler via `fl_Task` or `fh_Type`, noting that locks and filehandles share the same underlying representation there, and the lxa path now preserves that public contract with the hosted access-mode bridge: valid selectors still succeed through the same DOS entry point, shared-to-exclusive conflicts still fail with `ERROR_OBJECT_IN_USE`, invalid selectors still report `ERROR_BAD_NUMBER`, and the direct coverage in `Tests/Dos/ChangeMode`, `dos_gtest`, and `Tests/Exec/Library` keeps the intentionally stricter lxa-side object/new-mode validation explicit
- [x] Implement `ErrorReport()` per RKRM/NDK semantics with direct regression coverage; `ErrorReport()` now stops hitting the public stub by recognizing the documented DOS error set, returning `DOSTRUE` immediately for unknown codes or disabled requester mode, formatting the supported requester text from lock/filehandle/volume context when available, and routing the actual requester path through Intuition `AutoRequest()` when a screen/window context exists; direct coverage now lives in `Tests/Dos/ErrorReport`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `ErrorReport()` behavior against the AROS implementation; AROS `rom/dos/errorreport.c` recognizes the same documented DOS error set, returns `DOSTRUE` immediately for unknown codes, disabled requesters, and unsupported report payloads, derives volume/device context from `REPORT_STREAM`/`REPORT_LOCK`/`REPORT_VOLUME`/`REPORT_INSERT`, and routes the visible requester through the private `DisplayError()` helper with disk-insert notification flags for the mount/replace cases; the lxa path now preserves that public contract and context lookup while intentionally using the existing hosted name/device helpers plus direct Intuition `AutoRequest()` formatting instead of AROS's localized `DisplayError()` wrapper
- [x] Implement `GetConsoleTask()` per RKRM/NDK semantics with direct regression coverage; `GetConsoleTask()` now returns the current process `pr_ConsoleTask` directly, reports `NULL` for plain `Task` callers or processes without a console port, and adds direct regression coverage in `Tests/Dos/GetConsoleTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `GetConsoleTask()` behavior against the AROS implementation; AROS `rom/dos/getconsoletask.c` performs the same public check by calling `FindTask(NULL)`, returning `NULL` for plain `Task` callers via `__is_task(me)`, and otherwise returning the current process `pr_ConsoleTask` unchanged; the lxa implementation matches that externally visible contract with the equivalent `NT_PROCESS` gate and the existing direct regression coverage in `Tests/Dos/GetConsoleTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe keeps the alignment explicit
- [x] Implement `SetConsoleTask()` per RKRM/NDK semantics with direct regression coverage; `SetConsoleTask()` now matches the documented current-process contract by swapping `pr_ConsoleTask`, returning the previous console port, accepting `NULL` to clear the binding, and adding direct regression coverage in `Tests/Dos/SetConsoleTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SetConsoleTask()` behavior against the AROS implementation; AROS `rom/dos/setconsoletask.c` implements `SetConsoleTask()` by calling `FindTask(NULL)`, treating the caller as the current `Process`, swapping `pr_ConsoleTask`, and returning the previous console port, and the lxa path now preserves that public contract for real process callers while intentionally keeping the existing `NT_PROCESS` guard so accidental plain-`Task` callers fail safely instead of tripping AROS's internal assertion; the direct coverage in `Tests/Dos/SetConsoleTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe keeps the aligned swap/return semantics pinned down
- [x] Implement `GetFileSysTask()` per RKRM/NDK semantics with direct regression coverage; `GetFileSysTask()` now returns the current process `pr_FileSystemTask` directly, reports `NULL` for plain `Task` callers or processes without a filesystem port, and adds direct regression coverage in `Tests/Dos/GetFileSysTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `GetFileSysTask()` behavior against the AROS implementation; AROS `rom/dos/getfilesystask.c` implements `GetFileSysTask()` by calling `FindTask(NULL)`, asserting that the caller is a `Process`, and returning `pr_FileSystemTask` unchanged, and the lxa path now preserves that public contract while intentionally keeping the existing `NT_PROCESS` guard so plain `Task` callers fail safely with `NULL` instead of tripping an internal assertion; the aligned behavior remains covered by `Tests/Dos/GetFileSysTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Implement `SetFileSysTask()` per RKRM/NDK semantics with direct regression coverage; `SetFileSysTask()` now matches the documented current-process contract by swapping `pr_FileSystemTask`, returning the previous filesystem port, accepting `NULL` to clear the binding, and adding direct regression coverage in `Tests/Dos/SetFileSysTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SetFileSysTask()` behavior against the AROS implementation; AROS `rom/dos/setfilesystask.c` implements `SetFileSysTask()` by calling `FindTask(NULL)`, asserting that the caller is a `Process`, swapping `pr_FileSystemTask`, and returning the previous filesystem port, and the lxa path now preserves that public contract for real process callers while intentionally keeping the existing `NT_PROCESS` guard so accidental plain-`Task` callers fail safely with `NULL` instead of tripping AROS's internal assertion; the aligned swap/clear/restore semantics remain covered by `Tests/Dos/SetFileSysTask`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Implement `GetArgStr()` per RKRM/NDK semantics with direct regression coverage; `GetArgStr()` now returns the current process `pr_Arguments` pointer directly, reports `NULL` for plain `Task` callers, and adds direct regression coverage in `Tests/Dos/GetArgStr`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `GetArgStr()` behavior against the AROS implementation; AROS `rom/dos/getargstr.c` implements `GetArgStr()` by calling `FindTask(NULL)`, asserting that the caller is a `Process`, and returning `pr_Arguments` unchanged, and the lxa path now preserves that public contract for real process callers while intentionally keeping the existing `NT_PROCESS` guard so plain `Task` callers fail safely with `NULL` instead of tripping an internal assertion; the aligned pointer-return semantics remain covered by `Tests/Dos/GetArgStr`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Implement `SetArgStr()` per RKRM/NDK semantics with direct regression coverage; `SetArgStr()` now matches the documented current-process swap contract by returning the previous `pr_Arguments` pointer, accepting `NULL`, reporting `NULL` for plain `Task` callers, and adding direct regression coverage in `Tests/Dos/SetArgStr`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SetArgStr()` behavior against the AROS implementation; AROS `rom/dos/setargstr.c` implements `SetArgStr()` by calling `FindTask(NULL)`, asserting that the caller is a `Process`, swapping `pr_Arguments`, and returning the previous pointer, and the lxa path now preserves that public contract for real process callers while intentionally keeping the existing `NT_PROCESS` guard so plain `Task` callers fail safely with `NULL` instead of tripping an internal assertion; the aligned swap/clear/restore semantics remain covered by `Tests/Dos/SetArgStr`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Implement `RemDosEntry()` per RKRM/NDK semantics with direct regression coverage; `RemDosEntry()` now unlinks matching DOS list nodes from the hosted `di_DevInfo` chain without freeing caller-owned memory, safely rejects `NULL` or missing entries, and adds direct regression coverage in `Tests/Dos/RemDosEntry`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `RemDosEntry()` behavior against the AROS implementation; AROS `rom/dos/remdosentry.c` removes DOS list nodes by locking the list, walking predecessor links until `dol_Next` matches the target, and unlinking the node without freeing caller-owned memory, and the lxa path now preserves that public contract for successful removals while intentionally adding safer `NULL` and missing-entry rejection instead of assuming the caller always passes a live list member
- [x] Implement `AddDosEntry()` per RKRM/NDK semantics with direct regression coverage; `AddDosEntry()` now inserts DOS list entries under the hosted `di_DevInfo` semaphore, prepends successful additions, rejects duplicate device/assign names case-insensitively with `ERROR_OBJECT_EXISTS`, allows overlapping volume names as documented, and adds direct regression coverage in `Tests/Dos/AddDosEntry`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `AddDosEntry()` behavior against the AROS implementation; AROS `rom/dos/adddosentry.c` scans the existing DOS list for case-insensitive device/assign name clashes, prepends successful inserts, and leaves volume-name overlap unrestricted while serializing concurrent adders through DOS-list locking, and the lxa path now preserves that public contract with the hosted `di_DevLock` semaphore and the same non-volume duplicate rejection while intentionally keeping the stricter documented `NULL`-argument failure (`ERROR_REQUIRED_ARG_MISSING`) instead of AROS's legacy no-op success for `NULL` entries
- [x] Implement `MakeDosEntry()` per RKRM/NDK semantics with direct regression coverage; `MakeDosEntry()` now allocates a cleared `DosList`, creates a correctly terminated BCPL name string with the documented 255-byte cap, preserves the requested entry type, rejects `NULL` names with `ERROR_REQUIRED_ARG_MISSING`, and adds direct regression coverage in `Tests/Dos/MakeDosEntry`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `MakeDosEntry()` behavior against the AROS implementation; AROS `rom/dos/makedosentry.c` allocates a cleared `DosList`, allocates BCPL-name storage, stores the requested `dol_Type`, and returns the initialized node, and the lxa path now preserves that public contract while intentionally tightening two edge cases to match the NDK autodoc more safely: `NULL` names fail with `ERROR_REQUIRED_ARG_MISSING` instead of dereferencing through `strlen()`, and names longer than 255 bytes are both length-capped and copy-capped so the stored BSTR stays internally consistent instead of advertising a truncated BCPL length while `strcpy()` copies the full C string payload
- [x] Implement `FreeDosEntry()` per RKRM/NDK semantics with direct regression coverage; `FreeDosEntry()` now matches the documented `MakeDosEntry()` companion contract by treating `NULL` as a no-op, freeing the BCPL name payload before releasing the `DosList`, and adding direct regression coverage in `Tests/Dos/FreeDosEntry`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `FreeDosEntry()` behavior against the AROS implementation; AROS `rom/dos/freedosentry.c` implements `FreeDosEntry()` as a documented no-op for `NULL` pointers and otherwise frees the BCPL name payload with `FreeVec(BADDR(dol_Name))` before releasing the `DosList` itself, and the lxa path now preserves that public contract exactly while the existing direct regression coverage in `Tests/Dos/FreeDosEntry`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe keeps the aligned no-op and `IoErr()`-preservation semantics pinned down
- [x] Implement `Format()` per RKRM/NDK semantics with direct regression coverage; `Format()` now validates the documented device-name and volume-name syntax, rejects assign targets so only real devices proceed, routes packet-capable handlers through `ACTION_FORMAT`, cleanly reports `ERROR_ACTION_NOT_KNOWN` for the current hosted lock-only device bridge instead of tripping the old stub, and adds direct regression coverage in `Tests/Dos/Format`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `Format()` behavior against the AROS implementation; AROS `rom/dos/format.c` resolves the target through `getdevpacketinfo()`, thereby rejecting non-device DOS list entries, converts the requested volume name to a BCPL string, and forwards `ACTION_FORMAT` with that name plus the requested DOS type to the device handler port, and the lxa path now preserves that public contract through `_dos_GetDeviceProc()`, `lxa_dos_alloc_name_bstr()`, and `_dos_DoPkt(... ACTION_FORMAT ...)` while intentionally tightening the documented syntax checks for missing `:` in the device name and `:` characters in the volume name before device lookup; the existing `Tests/Dos/Format`, `dos_gtest`, and `Tests/Exec/Library` coverage keeps both the aligned handler-dispatch path and the current hosted `ERROR_ACTION_NOT_KNOWN` fallback explicit
- [x] Implement `Relabel()` per RKRM/NDK semantics with direct regression coverage; `Relabel()` now validates the documented drive/new-name syntax, rejects assign targets so only real devices proceed, routes packet-capable handlers through `ACTION_RENAME_DISK`, cleanly reports `ERROR_ACTION_NOT_KNOWN` for the current hosted lock-only device bridge instead of tripping the old stub, and adds direct regression coverage in `Tests/Dos/Relabel`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `Relabel()` behavior against the AROS implementation; AROS `rom/dos/relabel.c` delegates to `getdevpacketinfo()`, which resolves only real `DLT_DEVICE` targets through `GetDeviceProc()`, converts the requested new label to a BCPL string, and forwards `ACTION_RENAME_DISK` to the handler port, and the lxa path now preserves that public contract through `_dos_GetDeviceProc()`, `lxa_dos_alloc_name_bstr()`, and `_dos_DoPkt(... ACTION_RENAME_DISK ...)` while intentionally tightening the documented syntax checks for missing `:` in the drive name, `:` characters in the new label, and `NULL` arguments before device lookup; the existing `Tests/Dos/Relabel`, `dos_gtest`, and `Tests/Exec/Library` coverage keeps both the aligned real-device packet flow and the current hosted `ERROR_ACTION_NOT_KNOWN` fallback explicit
- [x] Implement `Inhibit()` per RKRM/NDK semantics with direct regression coverage; `Inhibit()` now validates the documented device-name syntax, rejects assign targets so only real devices proceed, routes packet-capable handlers through `ACTION_INHIBIT`, cleanly reports `ERROR_ACTION_NOT_KNOWN` for the current hosted lock-only device bridge instead of tripping the old stub, and adds direct regression coverage in `Tests/Dos/Inhibit`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `Inhibit()` behavior against the AROS implementation; AROS `rom/dos/inhibit.c` resolves the target through `getdevpacketinfo()`, which restricts the call to real `DLT_DEVICE` entries returned by `GetDeviceProc()`, and then forwards `ACTION_INHIBIT` with the caller's `onoff` flag to the handler port via `dopacket1()`; the lxa path now preserves that public contract through `_dos_GetDeviceProc()`, the existing assign rejection, and `_dos_DoPkt(... ACTION_INHIBIT ...)` while intentionally tightening the documented syntax checks for `NULL` names and missing `:` before device lookup, and the existing `Tests/Dos/Inhibit`, `dos_gtest`, and `Tests/Exec/Library` coverage keeps both the aligned handler-dispatch path and the current hosted `ERROR_ACTION_NOT_KNOWN` fallback explicit
- [x] Implement `AddBuffers()` per RKRM/NDK semantics with direct regression coverage; `AddBuffers()` now validates the documented device-name syntax, rejects assign targets so only real devices proceed, routes packet-capable handlers through `ACTION_MORE_CACHE`, preserves the legacy positive-buffer-count success quirk by normalizing positive handler returns to `DOSTRUE` while storing the real count in `IoErr()`, cleanly reports `ERROR_ACTION_NOT_KNOWN` for the current hosted lock-only device bridge instead of tripping the old stub, and adds direct regression coverage in `Tests/Dos/AddBuffers`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `AddBuffers()` behavior against the AROS implementation; AROS `rom/dos/addbuffers.c` resolves the target through `GetDeviceProc()`, rejects non-device DOS list entries with `ERROR_DEVICE_NOT_MOUNTED`, forwards `ACTION_MORE_CACHE` to the handler port, and preserves the legacy positive-return quirk by translating counts into `DOSTRUE` while storing the real count in `IoErr()`; the lxa path now preserves that public contract through `_dos_GetDeviceProc()`, assign rejection, `_dos_DoPkt(... ACTION_MORE_CACHE ...)`, and the same success normalization while intentionally tightening the documented syntax checks for `NULL` names and missing `:` before device lookup, with the existing `Tests/Dos/AddBuffers`, `dos_gtest`, and `Tests/Exec/Library` coverage keeping both the aligned handler-dispatch path and the current hosted `ERROR_ACTION_NOT_KNOWN` fallback explicit
- [x] Implement `AddSegment()` per RKRM/NDK semantics with direct regression coverage; `AddSegment()` now allocates and prepends resident `struct Segment` nodes in the hosted DOS `di_ResList`, stores the caller seglist plus copied BCPL name, rejects missing arguments and same-namespace case-insensitive duplicates with the documented `IoErr()` values, and adds direct regression coverage in `Tests/Dos/AddSegment`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `AddSegment()` behavior against the AROS implementation; AROS `rom/dos/addsegment.c` locks the resident segment list with `Forbid()`, rejects duplicates by calling `FindSegment()` case-insensitively within the user vs system namespace split, allocates a new `struct Segment`, copies the caller name into the inline BSTR payload, and prepends the node to `di_ResList`, and the lxa path now preserves that public contract with the same namespace-sensitive duplicate check, copied inline BCPL name, and head insertion while intentionally tightening the documented edge cases by rejecting `NULL` names or seglists with `ERROR_REQUIRED_ARG_MISSING` and capping copied names at 255 bytes so the resident BSTR stays consistent; the aligned behavior remains covered by `Tests/Dos/AddSegment`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Implement `ReadItem()` per RKRM/NDK semantics with direct regression coverage; `ReadItem()` now closes the public stub by matching the documented item parser for `CSource` and `Input()` streams, preserving the legacy separator/unread quirks plus quoted `*n`/`*e`/escaped-character handling that `ReadArgs()` compatibility depends on, and adds direct regression coverage in `Tests/Dos/ReadItem`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `ReadItem()` behavior against the AROS implementation; AROS `rom/dos/readitem.c` implements the documented `CSource`/`Input()` parser with the same intentionally preserved bugs called out in the NDK autodoc: space and `=` separators are not unread for unquoted items, `CSource` EOF/newline handling can rewind the last item character, quoted `*n`/`*e` and generic `*x` escapes are converted in-place, `IoErr()` stays untouched, and `maxchars == 0` still writes a NUL byte; the lxa path now matches that public contract and keeps the same compatibility quirks covered directly by `Tests/Dos/ReadItem`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Implement `CliInitNewcli()` per RKRM/NDK semantics with direct regression coverage; `CliInitNewcli()` now stops tripping the public stub by accepting the documented NewCLI-style startup packet, allocating/resetting the caller CLI when needed, wiring packet-provided current-dir/input/output state into the process and CLI fields, restoring the default `%N> ` prompt plus empty command strings, and adding direct regression coverage in `Tests/Dos/CliInitNewcli`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `CliInitNewcli()` behavior against the AROS implementation; AROS `rom/dos/cliinitnewcli.c` is just a thin wrapper around `internal_CliInitAny()`, where the NewCLI path is selected by `dp_Res1 != 0`, consumes `dp_Arg1` as the new current directory plus `dp_Arg2`/`dp_Arg3`/`dp_Arg4` as the standard/current streams, synthesizes missing output from the interactive-vs-`NIL:` console choice, resets the default `%N> ` prompt and empty command strings, and returns success with zero flags plus `IoErr()==0`; the lxa path now preserves that public NewCLI contract and keeps the aligned coverage in `Tests/Dos/CliInitNewcli`, `dos_gtest`, and `Tests/Exec/Library`, while intentionally remaining stricter about rejecting a `NULL` startup packet instead of inheriting AROS's internal helper assumption that the caller always provides one
- [x] Implement `CliInitRun()` per RKRM/NDK semantics with direct regression coverage; `CliInitRun()` now closes the public stub by initializing RUN-style shells from the startup packet, allocating a CLI on demand, wiring the current directory plus input/output streams into the process and CLI state, inheriting prompt/path context from the caller CLI when provided, returning the documented RUN flag bits for synthesized output and caller-supplied input, and adding direct regression coverage in `Tests/Dos/CliInitRun`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `CliInitRun()` behavior against the AROS implementation; AROS `rom/dos/cliinitrun.c` is the same thin wrapper pattern as `CliInitNewcli()`, selecting the shared `internal_CliInitAny()` RUN path with `dp_Res1 == 0`, treating `dp_Arg1` as the parent CLI plus `dp_Arg5` as the new current directory, honoring `dp_Arg2`/`dp_Arg3`/`dp_Arg4` and the `dp_Arg6` user-input flag, synthesizing missing output from the current console or `NIL:`, inheriting the parent prompt and current-dir name when an old CLI is supplied, and wiring the process stream fields from the resolved handles; the lxa path now preserves that public RUN packet contract and keeps the aligned coverage in `Tests/Dos/CliInitRun`, `dos_gtest`, and `Tests/Exec/Library`, while intentionally remaining narrower than AROS's private helper by omitting its AROS-only `dp_Arg7` standard-error extension, command-path cloning, and `SystemTagList()`-specific CLI type handling that is outside the public `CliInitRun()` surface here
- [x] Implement `SameDevice()` per RKRM/NDK semantics with direct regression coverage; `SameDevice()` now closes the public stub by treating null locks as `DOSFALSE`, delegating real comparisons through the existing hosted `SameLock()` bridge so same-object and same-volume locks both report `DOSTRUE` while cross-device locks stay false, and adding direct regression coverage in `Tests/Dos/SameDevice`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SameDevice()` behavior against the AROS implementation; AROS `rom/dos/samedevice.c` implements `SameDevice()` as a null-lock guard plus a direct comparison of the two `FileLock` structures' `fl_Volume` and `fl_Task` fields, and the lxa path now preserves that public contract by returning `DOSFALSE` for `BNULL` inputs and delegating real comparisons through the existing hosted `SameLock()` bridge, where both `LOCK_SAME` and `LOCK_SAME_VOLUME` map back to `DOSTRUE` while cross-device locks stay false; the existing `Tests/Dos/SameDevice`, `dos_gtest`, and `Tests/Exec/Library` coverage keeps the aligned duplicate-lock, same-volume, cross-device, and null-lock cases explicit
- [x] Implement `SetOwner()` per RKRM/NDK semantics with direct regression coverage; `SetOwner()` now closes the public stub by resolving the target path through the hosted DOS bridge, persisting the 32-bit owner value as hosted metadata for later `Examine()`/`ExNext()`/`ExamineFH()` reads, rejecting missing names and missing objects with the documented `IoErr()` values, and adding direct regression coverage in `Tests/Dos/SetOwner`, `dos_gtest`, and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SetOwner()` behavior against the AROS implementation; AROS `rom/dos/setowner.c` resolves the target through `getpacketinfo()`, then forwards `ACTION_SET_OWNER` with the resolved lock/path pair and caller-supplied 32-bit owner value to the handler port via `dopacket4()`, and the lxa path now preserves that public contract for successful path resolution and missing-object failure while intentionally using the existing hosted DOS bridge plus persisted owner metadata instead of a packet-capable filesystem handler so later `Examine()`/`ExNext()`/`ExamineFH()` reads observe the same owner value without reopening the old stub path

### Phase 84: Close `utility.library` stub surface

Goal: eliminate the remaining non-private `utility.library` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [x] Implement `PackBoolTags()` per RKRM/NDK semantics with direct regression coverage; `PackBoolTags()` now closes the public stub by iterating the caller tag list through `NextTagItem()`, looking up boolean mappings via `FindTagItem()`, clearing mapped bits when `ti_Data == 0`, setting them otherwise, and keeping direct regression coverage in `tests/utility/tagitems` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `PackBoolTags()` behavior against the AROS implementation; AROS `rom/utility/packbooltags.c` implements `PackBoolTags()` as the same `NextTagItem()` walk over `tagList`, applying the last matching boolean tag through `FindTagItem()` and clearing-vs-setting `initialFlags` based on `ti_Data == 0`, and the lxa path now preserves that public contract exactly with the existing direct regression coverage in `tests/utility/tagitems` and `Tests/Exec/Library`
- [x] Implement `FilterTagChanges()` per RKRM/NDK semantics with direct regression coverage; `FilterTagChanges()` now walks `changeList` through `NextTagItem()`, marks unchanged tags as `TAG_IGNORE`, optionally writes changed values back into `originalList` when `apply` is non-zero, safely treats either null list as a no-op, and adds direct regression coverage in `tests/utility/tagitems` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `FilterTagChanges()` behavior against the AROS implementation; AROS `rom/utility/filtertagchanges.c` implements `FilterTagChanges()` as the same `NextTagItem()` walk over `changeList`, using `FindTagItem()` to locate matching original tags, rewriting unchanged entries to `TAG_IGNORE`, and conditionally copying changed `ti_Data` values back into `originalList` only when `apply` is non-zero, and the lxa path now preserves that public contract exactly, including the documented null-list no-op behavior, with the aligned regression coverage remaining in `tests/utility/tagitems` and `Tests/Exec/Library`
- [x] Implement `ApplyTagChanges()` per RKRM/NDK semantics with direct regression coverage; `ApplyTagChanges()` now closes the public stub by walking the destination list through `NextTagItem()`, replacing each matched tag's `ti_Data` from the first matching entry found in `changeList`, safely treating either null list as a no-op, and adding direct regression coverage in `tests/utility/tagitems` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `ApplyTagChanges()` behavior against the AROS implementation; AROS `rom/utility/applytagchanges.c` walks the destination list inline, handling `TAG_IGNORE`/`TAG_SKIP`/`TAG_MORE` explicitly and replacing each matched tag's `ti_Data` from the first `FindTagItem()` hit in `changeList`, and the lxa path now preserves that public contract by delegating the same control-tag traversal to `NextTagItem()` before applying the first matching replacement, while intentionally keeping the documented null-list no-op behavior instead of inheriting AROS's internal assumption that both pointers are non-NULL; the aligned semantics remain covered by `tests/utility/tagitems` and `Tests/Exec/Library`

### Phase 85 complete (`0.8.37`): Close `graphics.library` stub surface

Goal: eliminate the remaining non-private `graphics.library` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [x] Implement `DoCollision()` per RKRM/NDK semantics with direct regression coverage; `graphics.library` now walks the sorted GEL list for boundary and gel-to-gel collisions, dispatches boundary hit callbacks with `TOPHIT`/`BOTTOMHIT`/`LEFTHIT`/`RIGHTHIT`, checks overlapping collision masks in sorted pair order, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `DoCollision()` behavior against the AROS implementation; AROS `rom/graphics/docollision.c` scans the sorted GEL list, tests boundary callbacks first, then intersects `CollMask` rows for vertically overlapping pairs and dispatches the first matching collision vector from `MeMask & HitMask`, and the lxa path now preserves that public contract while intentionally using clearer per-pixel overlap helpers instead of AROS's word-shifted scan loop
- [x] Implement `InitMasks()` per RKRM/NDK semantics with direct regression coverage; `InitMasks()` now builds the default `CollMask` by OR-ing image planes and then builds `BorderLine` by OR-ing each collision-mask column across the sprite height, with direct regression coverage in `Tests/Graphics/SpritesGels` and the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `InitMasks()` behavior against the AROS implementation; AROS `rom/graphics/initmasks.c` derives `WordsPerLine` from the VSprite/Bob layout, ORs all image planes into `CollMask`, and then ORs rows into `BorderLine`, and the lxa implementation now preserves that public contract while intentionally honoring the explicit `Depth` field when present so single-plane regression fixtures remain valid without depending on the legacy hard-coded VSprite depth assumption
- [x] Implement `RemIBob()` per RKRM/NDK semantics with direct regression coverage; `graphics.library` now immediately clears the target Bob plus any later overlapping Bobs from the current RastPort, unlinks the Bob's VSprite from the active GEL list, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `RemIBob()` behavior against the AROS implementation; AROS `rom/graphics/remibob.c` routes `RemIBob()` through `_ClearBobAndFollowClearPath()` before unlinking the Bob with `RemVSprite()`, and the lxa path now preserves that public contract by immediately clearing the target Bob and any later overlapping Bobs, marking them for redraw on the next `DrawGList()`, and then unlinking the removed Bob while intentionally using a direct overlap walk instead of AROS's internal ClearPath recursion because lxa does not yet build the same draw/clear-path bookkeeping
- [x] Implement `SetCollision()` per RKRM/NDK semantics with direct regression coverage; `SetCollision()` now stores the caller-provided boundary or gel-collision routine in the requested `collHandler->collPtrs[]` slot, leaves unrelated vector entries untouched, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SetCollision()` behavior against the AROS implementation; AROS `rom/graphics/setcollision.c` implements `SetCollision()` as a direct `collHandler->collPtrs[num] = routine` store, and the lxa path now preserves that public contract for valid `0..15` vector indices while intentionally keeping the existing null-pointer/range guards so invalid callers fail safely instead of scribbling past the table
- [x] Implement `AddAnimOb()` per RKRM/NDK semantics with direct regression coverage; `AddAnimOb()` now prepends the caller's `AnimOb` onto the `AnimKey` chain, fixes the first/last `PrevOb`/`NextOb` links, initializes every active component timer from `TimeSet`, queues each component Bob through `AddBob()`, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `AddAnimOb()` behavior against the AROS implementation; AROS `rom/graphics/addanimob.c` implements `AddAnimOb()` as the same head insertion plus per-component `Timer = TimeSet` initialization followed by `AddBob()` for each component on the `NextComp` chain, and the lxa path now preserves that public contract while intentionally keeping the existing null-argument guards so invalid callers fail safely instead of dereferencing `NULL`
- [x] Implement `Animate()` per RKRM/NDK semantics with direct regression coverage; `Animate()` now advances each `AnimOb` clock/position/velocity in the documented fixed-point domain, dispatches optional `AnimORoutine`/`AnimCRoutine` callbacks with the owning object/component pointer, switches timed-out components onto their `NextSeq` while preserving the live component chain, applies `RINGTRIGGER` translations, updates current/new `VSprite` coordinates with the RKRM rounding rule, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `Animate()` behavior against the AROS implementation; AROS `rom/graphics/animate.c` implements `Animate()` as the same per-`AnimOb` clock and fixed-point motion update followed by optional object/component callbacks, timed sequence rollover onto `NextSeq`, `RINGTRIGGER` translation, and `RemBob()` retirement of the completed sequence while computing `VSprite` coordinates with the legacy half-step rounding rule, and the lxa path now preserves that public contract while intentionally keeping the existing null-`anKey` guard so invalid callers fail safely instead of dereferencing `NULL`
- [x] Implement `GetGBuffers()` per RKRM/NDK semantics with direct regression coverage; `GetGBuffers()` now walks every component and ring-linked sequence in the caller's `AnimOb`, allocates `ImageShadow`/`CollMask`, `SaveBuffer`, `BorderLine`, and optional double-buffer `DBufPacket`/`BufBuffer` storage sized from the live `VSprite` geometry, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `GetGBuffers()` behavior against the AROS implementation; AROS `rom/graphics/getgbuffers.c` performs the same per-component/per-sequence walk, allocates `ImageShadow` first, aliases `CollMask` to that storage, allocates `SaveBuffer` and `BorderLine`, and conditionally allocates `DBufPacket` plus `BufBuffer` when double buffering is requested, and the lxa path now preserves that public contract while intentionally using the existing `graphics_vsprite_words_per_line()` and `graphics_vsprite_mask_depth()` helpers so Bob vs VSprite sizing stays consistent with the adjacent `InitMasks()` logic
- [x] Implement `InitGMasks()` per RKRM/NDK semantics with direct regression coverage; `InitGMasks()` now walks each `AnimOb` component plus every ring-linked sequence and forwards their live `BobVSprite` definitions through `InitMasks()`, with direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `InitGMasks()` behavior against the AROS implementation; AROS `rom/graphics/initgmasks.c` implements `InitGMasks()` as the same component walk plus per-sequence ring traversal that simply calls `InitMasks()` on each sequence Bob's `BobVSprite`, and the lxa path now preserves that public contract while intentionally keeping the existing null-`AnimOb` and null-`AnimBob`/`BobVSprite` guards so invalid callers fail safely instead of dereferencing through broken animation state
- [x] Implement `CBump()` per RKRM/NDK semantics with direct regression coverage; `CBump()` now advances the active `UCopList` write pointer, spills the final in-block instruction into a chained 10-entry extension block when the current block fills, preserves the documented no-status-on-allocation-failure behavior, and adds direct regression coverage in `Tests/Graphics/DisplayViewPort` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `CBump()` behavior against the AROS implementation; AROS `rom/graphics/cbump.c` increments the active block count, allocates or reuses a chained 10-entry `CopList` when the current block fills, copies the last in-progress instruction into that next block, and replaces the old slot with `CPRNXTBUF`, and the lxa path now preserves that public contract while intentionally keeping the existing null-pointer guard so invalid callers fail safely instead of dereferencing through a missing `UCopList`
- [x] Implement `CMove()` per RKRM/NDK semantics with direct regression coverage; `CMove()` now writes the active user-copper slot in place with `COPPER_MOVE`, destination register, and 16-bit immediate data, reports success while the current `CopList` still has room, reports failure once the current block is full, and adds direct regression coverage in `Tests/Graphics/DisplayViewPort` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `CMove()` behavior against the AROS implementation; AROS `rom/graphics/cmove.c` implements `CMove()` as the same in-place write to `ucl->CopList->CopPtr` with `COPPER_MOVE`, `DestAddr`, and `DestData`, and the lxa path now preserves that public contract while intentionally adding the documented V47-style boolean success/failure result so callers can detect a full user copper block before `CBump()` extends it
- [x] Implement `CWait()` per RKRM/NDK semantics with direct regression coverage; `CWait()` now writes the active user-copper slot in place with `COPPER_WAIT` plus the requested vertical and horizontal beam positions, leaves the current block untouched once it is full, and adds direct regression coverage in `Tests/Graphics/DisplayViewPort` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `CWait()` behavior against the AROS implementation; AROS `rom/graphics/cwait.c` implements `CWait()` as the same in-place write to `ucl->CopList->CopPtr` with `COPPER_WAIT`, `VWaitPos`, and `HWaitPos`, and the lxa path now preserves that public contract while intentionally keeping the existing null-pointer and full-block guards so invalid or already-exhausted copper lists fail safely instead of dereferencing through missing state or overwriting past the active block
- [x] Implement `SyncSBitMap()` per RKRM/NDK semantics with direct regression coverage; `graphics.library` now walks the visible `ClipRect` list for `LAYERSUPER` layers, copies each on-screen region from the layer RastPort bitmap back into the backing `SuperBitMap` using the documented scroll-adjusted coordinates, and adds direct regression coverage in `Tests/Layers/CoreOps` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SyncSBitMap()` behavior against the AROS implementation; AROS `rom/graphics/syncsbitmap.c` implements `SyncSBitMap()` as a `ClipRect` walk that ignores obscured rectangles and blits each visible region from `l->rp->BitMap` into `l->SuperBitMap` at `bounds - layer origin - scroll`, and the lxa path now preserves that public contract while intentionally using the existing `cr->obscured == 0` visibility test instead of AROS's older `lobs == NULL` field spelling
- [x] Implement `CopySBitMap()` per RKRM/NDK semantics with direct regression coverage; `CopySBitMap()` now walks the visible `ClipRect` list for `LAYERSUPER` layers, copies each backing `SuperBitMap` region into the on-screen layer RastPort bitmap using the documented layer-origin and scroll-adjusted coordinates, and adds direct regression coverage in `Tests/Layers/CoreOps` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `CopySBitMap()` behavior against the AROS implementation; AROS `rom/graphics/copysbitmap.c` implements `CopySBitMap()` as the inverse `ClipRect` walk of `SyncSBitMap()`, ignoring obscured rectangles and blitting each visible region from `l->SuperBitMap` into `l->rp->BitMap` at `bounds - layer origin - scroll`, and the lxa path now preserves that public contract while intentionally using the existing `cr->obscured == 0` visibility test instead of AROS's older `lobs == NULL` field spelling
- [x] Implement `FreeGBuffers()` per RKRM/NDK semantics with direct regression coverage; `FreeGBuffers()` now walks every AnimOb component/sequence buffer allocated by `GetGBuffers()`, frees `ImageShadow`, `CollMask`, `SaveBuffer`, `BorderLine`, and optional double-buffer storage in place, clears the public pointers afterward, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `FreeGBuffers()` behavior against the AROS implementation; AROS `rom/graphics/freegbuffers.c` walks the same AnimOb component/sequence rings, frees `ImageShadow`, a distinct `CollMask`, `SaveBuffer`, `BorderLine`, and optional `DBufPacket`/`BufBuffer`, then clears the exposed pointers, and the lxa path now preserves that public contract while intentionally tolerating missing Bob/VSprite links and zero-sized helper results so partially initialized caller data fails safely instead of dereferencing through invalid sequence state
- [x] Implement `CalcIVG()` per RKRM/NDK semantics with direct regression coverage; `CalcIVG()` now counts the `vp->DspIns` copper instructions, converts MOVE/WAIT opcodes into copper cycle cost, subtracts viewport bitplane DMA bandwidth from the per-line copper budget, returns the required blank-line count in viewport-resolution lines, and adds direct regression coverage in `tests/graphics/display_viewport` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `CalcIVG()` behavior against the AROS implementation; the current AROS `rom/graphics/calcivg.c` still returns the unimplemented-stub result `0`, so the lxa path instead follows the V39 autodoc and Amiga copper timing model explicitly by basing the result on `vp->DspIns` only, charging WAIT vs MOVE instructions separately, accounting for bitplane bandwidth and lace halving, and keeping that intended divergence pinned down with the new direct regression coverage
- [x] Implement `SetChipRev()` per RKRM/NDK semantics with direct regression coverage; `SetChipRev()` now updates `GfxBase->ChipRevBits0` to the requested documented chipset level when that level is available, preserves the current best-supported AGA capability for `SETCHIPREV_BEST` and unsupported requests, and adds direct regression coverage in `Tests/Graphics/ColorsPens` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `SetChipRev()` behavior against the AROS implementation; AROS `arch/m68k-amiga/graphics/setchiprev.c` probes the live custom chips to derive the best available chipset, then clamps explicit `SETCHIPREV_A`/`_ECS`/`_AA` requests down to that available level before storing `GfxBase->ChipRevBits0`, and the lxa path now preserves that public contract by exposing a hosted AGA-capable machine model whose best level is `SETCHIPREV_AA` while keeping the same request clamping and return-value semantics covered by the new regressions
- [x] Implement `GetExtSpriteA()` per RKRM/NDK semantics with direct regression coverage; `GetExtSpriteA()` now allocates single sprites or even/odd attached pairs from the shared eight-slot hardware sprite pool, honors `GSTAG_SPRITE_NUM` for explicit requests, rejects invalid attached-pair requests, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `GetExtSpriteA()` behavior against the AROS implementation; AROS `rom/graphics/getextspritea.c` still returns `-1` unconditionally because its hosted graphics stack does not expose a general hardware sprite manager beyond the mouse-pointer path, while the lxa implementation follows the V39 autodoc instead by sharing the existing `GetSprite()` reservation bitmap across single and attached-pair requests, keeping the documented `GSTAG_SPRITE_NUM`/`GSTAG_ATTACHED` semantics covered explicitly in the new regressions
- [x] Implement `AllocSpriteDataA()` per RKRM/NDK semantics with direct regression coverage; `AllocSpriteDataA()` now validates the documented width/replication/output-height tag contract, converts planar bitmap or legacy sprite-image input into chip-resident sprite words plus owned bitmap backing, applies attached-source plane selection and V39 attach-bit semantics, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `AllocSpriteDataA()` behavior against the AROS implementation; AROS `rom/graphics/allocspritedataa.c` follows the same public tag surface for width/replication/output-height and legacy-format input, but keeps the converted pixels only in an owned bitmap object and explicitly leaves `SPRITEA_Attached` unsupported there, while the lxa path preserves the documented public contract needed by `ChangeSprite()`-style sprite consumers by materializing chip-resident spriteimage words, setting the V39 attach bit for attached pairs, rejecting impossible width/height requests, and keeping the aligned plus intentionally stricter attached-data behavior covered by `Tests/Graphics/SpritesGels` and `Tests/Exec/Library`
- [x] Implement `ChangeExtSpriteA()` per RKRM/NDK semantics with direct regression coverage; `ChangeExtSpriteA()` now validates the replacement sprite payload, preserves the existing sprite engine number and position from `oldsprite`, rejects conflicting or invalid sprite-number handoffs, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `ChangeExtSpriteA()` behavior against the AROS implementation; AROS `rom/graphics/changeextspritea.c` provides only a mouse-pointer-oriented implementation that copies the old position, requires a compatible sprite number, and fails for unsupported hardware/view cases, and the lxa path now preserves that public handoff contract for simple sprite-engine state while intentionally remaining viewport-agnostic until hosted hardware-sprite display plumbing grows beyond the current bookkeeping-only implementation
- [x] Implement `FreeSpriteData()` per RKRM/NDK semantics with direct regression coverage; `FreeSpriteData()` now treats `NULL` as a no-op, releases the owned backing bitmap plus chip-resident sprite words created by `AllocSpriteDataA()`, preserves `IoErr()`, and adds direct regression coverage in `Tests/Graphics/SpritesGels` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `FreeSpriteData()` behavior against the AROS implementation; AROS `rom/graphics/freespritedata.c` performs the same public `NULL`-safe teardown by freeing the owned bitmap before releasing the `ExtSprite` allocation, and the lxa path now preserves that contract while intentionally also freeing the separate chip-resident `posctldata` block because `AllocSpriteDataA()` materializes sprite words outside the wrapper struct instead of embedding all sprite payload in one `FreeVec()` allocation

### Phase 86: Close `intuition.library` stub surface

Goal: eliminate the remaining non-private `intuition.library` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [x] Implement `OpenIntuition()` per RKRM/NDK semantics with direct regression coverage; `OpenIntuition()` now follows the legacy public entry-point contract by ensuring the Workbench screen exists once, reusing the existing Workbench instance when already open, and adding direct regression coverage in `Tests/Intuition/ScreenManipulation` plus the `Tests/Exec/Library` entry-point probe
- [x] Compare and verify `OpenIntuition()` behavior against the AROS implementation; AROS marks `OpenIntuition()` as an AmigaOS-private entry point and omits a public implementation from its Intuition function table, while the NDK still exposes the legacy vector, so the lxa path intentionally preserves the observable Amiga contract by delegating to `OpenWorkBench()` only when no Workbench screen is present and keeping the aligned one-shot/idempotent semantics covered in `Tests/Intuition/ScreenManipulation` and `Tests/Exec/Library`
- [x] Implement `Intuition()` per RKRM/NDK semantics with direct regression coverage; `Intuition()` now accepts synthetic `InputEvent` chains instead of trapping, routes RAWKEY/RAWMOUSE/POINTERPOS compatibility events through the same Intuition-side dispatch used by live host input, updates the current mouse/time snapshot, and adds direct regression coverage in `Tests/Exec/Library` plus `Tests/Intuition/IDCMPInput`
- [ ] Compare and verify `Intuition()` behavior against the AROS implementation

### Phase 87: Close `icon.library` stub surface

Goal: eliminate the remaining non-private `icon.library` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [ ] Implement `PutDefDiskObject()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `PutDefDiskObject()` behavior against the AROS implementation
- [ ] Implement `IconControlA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `IconControlA()` behavior against the AROS implementation
- [ ] Implement `DrawIconStateA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `DrawIconStateA()` behavior against the AROS implementation
- [ ] Implement `GetIconRectangleA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `GetIconRectangleA()` behavior against the AROS implementation
- [ ] Implement `LayoutIconA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `LayoutIconA()` behavior against the AROS implementation
- [ ] Implement `ChangeToSelectedIconColor()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `ChangeToSelectedIconColor()` behavior against the AROS implementation

### Phase 88: Close `diskfont.library` stub surface

Goal: eliminate the remaining non-private `diskfont.library` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [ ] Implement `EOpenEngine()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `EOpenEngine()` behavior against the AROS implementation
- [ ] Implement `ECloseEngine()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `ECloseEngine()` behavior against the AROS implementation
- [ ] Implement `ESetInfoA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `ESetInfoA()` behavior against the AROS implementation
- [ ] Implement `EObtainInfoA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `EObtainInfoA()` behavior against the AROS implementation
- [ ] Implement `EReleaseInfoA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `EReleaseInfoA()` behavior against the AROS implementation
- [ ] Implement `OpenOutlineFont()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `OpenOutlineFont()` behavior against the AROS implementation
- [ ] Implement `CloseOutlineFont()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `CloseOutlineFont()` behavior against the AROS implementation
- [ ] Implement `WriteFontContents()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `WriteFontContents()` behavior against the AROS implementation
- [ ] Implement `WriteDiskFontHeaderA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `WriteDiskFontHeaderA()` behavior against the AROS implementation
- [ ] Implement `ObtainCharsetInfo()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `ObtainCharsetInfo()` behavior against the AROS implementation

### Phase 89: Close `workbench.library` stub surface

Goal: eliminate the remaining non-private `workbench.library` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [ ] Implement `UpdateWorkbench()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `UpdateWorkbench()` behavior against the AROS implementation
- [ ] Implement `WBInfo()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `WBInfo()` behavior against the AROS implementation
- [ ] Implement `OpenWorkbenchObjectA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `OpenWorkbenchObjectA()` behavior against the AROS implementation
- [ ] Implement `CloseWorkbenchObjectA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `CloseWorkbenchObjectA()` behavior against the AROS implementation
- [ ] Implement `WorkbenchControlA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `WorkbenchControlA()` behavior against the AROS implementation
- [ ] Implement `AddAppWindowDropZoneA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `AddAppWindowDropZoneA()` behavior against the AROS implementation
- [ ] Implement `RemoveAppWindowDropZone()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `RemoveAppWindowDropZone()` behavior against the AROS implementation
- [ ] Implement `ChangeWorkbenchSelectionA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `ChangeWorkbenchSelectionA()` behavior against the AROS implementation
- [ ] Implement `MakeWorkbenchObjectVisibleA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `MakeWorkbenchObjectVisibleA()` behavior against the AROS implementation
- [ ] Implement `WhichWorkbenchObjectA()` per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `WhichWorkbenchObjectA()` behavior against the AROS implementation

### Phase 90: Close `timer.device` stub surface

Goal: eliminate the remaining non-private `timer.device` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [ ] Implement `Expunge()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `Expunge()` behavior against the AROS `timer.device` implementation

### Phase 91: Close `clipboard.device` stub surface

Goal: eliminate the remaining non-private `clipboard.device` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [ ] Implement `Expunge()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `Expunge()` behavior against the AROS `clipboard.device` implementation
- [ ] Implement `CBD_POST` handling per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `CBD_POST` behavior against the AROS `clipboard.device` implementation
- [ ] Implement `CBD_CHANGEHOOK` handling per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `CBD_CHANGEHOOK` behavior against the AROS `clipboard.device` implementation
- [ ] Implement `AbortIO()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `AbortIO()` behavior against the AROS `clipboard.device` implementation

### Phase 92: Close `gameport.device` stub surface

Goal: eliminate the remaining non-private `gameport.device` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [ ] Finish `InitDev()` so `gameport.device` startup no longer behaves as a stub and its resident/device init path follows Amiga device-init expectations
- [ ] Compare and verify the finished `InitDev()` path against the AROS `gameport.device` initialization flow
- [ ] Implement `Open()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `Open()` behavior against the AROS `gameport.device` implementation
- [ ] Implement `Close()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `Close()` behavior against the AROS `gameport.device` implementation
- [ ] Implement `Expunge()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `Expunge()` behavior against the AROS `gameport.device` implementation
- [ ] Implement `BeginIO()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `BeginIO()` behavior against the AROS `gameport.device` implementation
- [ ] Implement `AbortIO()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `AbortIO()` behavior against the AROS `gameport.device` implementation

### Phase 93: Close `trackdisk.device` stub surface

Goal: eliminate the remaining non-private `trackdisk.device` stubs, match RKRM/NDK behavior, and keep each closure checked against AROS.

- [ ] Implement `Expunge()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `Expunge()` behavior against the AROS `trackdisk.device` implementation
- [ ] Implement `TD_RAWREAD` handling per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `TD_RAWREAD` behavior against the AROS `trackdisk.device` implementation
- [ ] Implement `TD_RAWWRITE` handling per RKRM/NDK semantics with direct regression coverage
- [ ] Compare and verify `TD_RAWWRITE` behavior against the AROS `trackdisk.device` implementation
- [ ] Implement `AbortIO()` per RKRM/NDK device semantics with direct regression coverage
- [ ] Compare and verify `AbortIO()` behavior against the AROS `trackdisk.device` implementation

### Phase 94: Close `mathffp.library` stub surface

Goal: eliminate the remaining non-private `mathffp.library` stub logging and keep the hosted math bridge aligned with Amiga library-init expectations.

- [ ] Finish `__g_lxa_mathffp_InitLib()` so mathffp library startup no longer logs an unimplemented stub and the resident init path follows Amiga library-init expectations
- [ ] Compare and verify the finished `__g_lxa_mathffp_InitLib()` path against the AROS `mathffp.library` initialization flow

### Phase 95: Stub-eradication closeout

Goal: finish the ROM-side stub elimination sweep cleanly and prove that only intentional private/reserved entry points remain unimplemented.

- [ ] Keep private/reserved entry points intentionally unimplemented, but ensure every such call path reports a descriptive `error: private function called` diagnostic instead of a generic stub message
- [ ] Rerun a full ROM-source stub scan and verify that no non-private unimplemented stub logs remain anywhere under `src/rom/`

---

## Completed Milestones

### Phase 78 complete (`0.8.0`)

Phase 78 is now closed as the major compatibility-release pass for `0.8.0`.

- [x] Completed the AROS verification sweep across the planned ROM libraries and devices, closing the tracked public API surface with direct regression coverage
- [x] Completed the FS-UAE review and documented the boundary between in-scope hosted AmigaOS compatibility and out-of-scope full hardware emulation
- [x] Folded the review fallout into future work instead of leaving the roadmap as a per-function checklist; Phase 80 now tracks the remaining architecture and performance cleanup
- [x] Ran the final regression/integration sweep, including targeted app-driver reruns and longer release validation
- [x] Synchronized the release as `0.8.0` across the build, version header, README, and roadmap

Phase 78 highlights, now intentionally compacted:

- [x] Core libraries closed: Exec, DOS, Graphics, Layers, Intuition, GadTools, ASL, Utility, Locale, IFFParse, Keymap, Diskfont, Workbench/Icon, and the planned math libraries
- [x] Device coverage closed: timer, console, input, audio, trackdisk, and expansion; `keyboard.device` is the next major gap and now stands alone as Phase 79
- [x] Third-party-library policy enforced: `commodities.library`, `rexxsyslib.library`, and `datatypes.library` stay disk-provided and off the ROM surface
- [x] Structural verification and compatibility regression coverage were expanded and kept green through the release close-out

### Earlier phases compacted

- [x] Phases 1-72: project foundation, host/ROM infrastructure, test migration to GTest + liblxa, build/test reliability, and broad compatibility groundwork
- [x] Phase 72.5: codebase audit pass completed
- [x] Phases 73-77: core resource management, DOS/VFS hardening, graphics/layers growth, Intuition/BOOPSI expansion, missing library/device coverage, and the lead-in work required for the final Phase 78 sweep

---

## Notes

- The roadmap now tracks only future phases plus compact milestone summaries; detailed completed checklists should stay in git history rather than dominating the live roadmap.
- Third-party libraries are not to be reimplemented in ROM; they must be supplied on disk and opened through the standard Amiga library search path.
